[build]
builder = "nixpacks"
buildCommand = "cd launch-ui && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "cd launch-ui && npm run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
